# outputs/ フォルダのルール
outputs/tmp/          # 一時作業ファイルはバージョン管理対象外
.DS_Store             # macOSシステムファイル
**/.DS_Store
*.local               # ローカル環境固有のファイル

# AIの記憶ファイル（ローカル専用・個人管理・リモートにpushしない）
context/
